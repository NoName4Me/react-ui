import Button, {ButtonProps} from '..';
import Icon from '../../Icon';
import Modal from '../../Modal';

import { Meta, Story, ArgsTable, Props, Stories, Preview } from '@storybook/addon-docs/blocks';

<Meta title="Doc/Button" component={Button}/>

# Button

提供基础的按钮组件: <Button size="small" onClick={()=>alert('hi')}>点我 alert</Button>

## 大小

可以通过 `size` 来控制 `Button` 大小，`type` 指定不同的风格：

export const Template = (args, {argTypes}) => <Button {...args} onClick={() => {}} props={argTypes}/>

<Preview>
  <Story id="story-button--base" />
</Preview>

## 禁用

`disabeld` 属性控制是否禁用按钮。

<Preview>
  <Story id="story-button--disable" />
</Preview>

## 自定义显示内容（如图标）

可以通过自定义内容定制出不同的组件。

<Preview>
  <Story id="story-button--customize" />
</Preview>

## API

<Story name="Controls" args={{ 
    size: 'normal',
    type: 'fill',
    children: '一个按钮'
  }}>
  {Template.bind({})}
</Story>

<ArgsTable story="Controls"/>
