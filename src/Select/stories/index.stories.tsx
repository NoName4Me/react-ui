import React from 'react';
import Select from '../index';

export default {
  title: 'Story/Select',
};

export const base = () => {
  return <Select>Select</Select>;
};

base.story = {
  name: '基础',
};
